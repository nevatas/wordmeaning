# 🚂 Настройка на Railway - Пошаговая инструкция

## ✅ У вас уже есть PostgreSQL сервис, теперь осталось:

### Шаг 1: Подключите базу данных к боту

1. Откройте ваш проект на Railway
2. Перейдите в **wordmeaning сервис** (ваш бот)
3. Откройте вкладку **Variables**
4. Найдите кнопку **+ New Variable** → **Add Reference**
5. Выберите **Postgres** сервис
6. Выберите переменную **DATABASE_URL**
7. Нажмите **Add**

### Шаг 2: Проверьте остальные переменные

Убедитесь, что в боте есть эти переменные:

```
✅ TELEGRAM_BOT_TOKEN - токен от @BotFather
✅ OPENROUTER_API_KEY - API ключ от OpenRouter  
✅ DATABASE_URL - ссылка на PostgreSQL (добавили в Шаге 1)
```

### Шаг 3: Задеплойте изменения

1. Сделайте `git push` с обновленным кодом
2. Railway автоматически обнаружит изменения
3. Начнется новый деплой
4. Подождите ~2-3 минуты

### Шаг 4: Проверка

1. Откройте логи бота на Railway
2. Вы должны увидеть: `"Bot is running..."`
3. Напишите боту в Telegram любое слово
4. Проверьте, что слово сохранилось (команда `/list`)
5. Сделайте еще один деплой (любой пуш) и проверьте, что слова не удалились! ✅

---

## 🎉 Готово!

Теперь ваши данные хранятся в отдельной PostgreSQL базе и **никогда не удаляются** при деплоях!

---

## 🔍 Возможные проблемы

### Ошибка "DATABASE_URL not found"

**Решение:** Проверьте, что переменная `DATABASE_URL` добавлена в сервис бота (не PostgreSQL сервис!)

### Ошибка подключения к базе

**Решение:** 
1. Проверьте, что PostgreSQL сервис запущен (Online)
2. Убедитесь, что вы добавили именно `DATABASE_URL` из PostgreSQL сервиса
3. Попробуйте сделать Restart бота

### Бот не отвечает после деплоя

**Решение:**
1. Откройте логи бота
2. Если видите ошибку с `psycopg2` - подождите завершения установки зависимостей
3. Если ошибка с `DATABASE_URL` - см. выше

---

## 💡 Полезные команды Railway CLI (опционально)

```bash
# Установка Railway CLI
npm i -g @railway/cli

# Логин
railway login

# Просмотр логов
railway logs

# Подключение к PostgreSQL
railway connect postgres
```
