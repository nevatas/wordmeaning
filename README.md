# Word Meaning Bot

Telegram-бот для изучения слов с использованием AI и интервальных повторений.

## Возможности

- 📖 Получение определений слов через AI (OpenRouter)
- 💾 Сохранение слов в PostgreSQL базу данных
- 🧠 Система интервальных повторений для эффективного запоминания
- 🇷🇺 Для английских слов: определение на русском, примеры на английском
- 🔄 Персистентное хранение данных между деплоями

## Команды

- `/start` - Начать работу с ботом
- `/train` - Начать сессию повторения слов
- Отправьте любое слово - получить определение и добавить в библиотеку

## Локальный запуск

1. Установите PostgreSQL локально или используйте Docker

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` с переменными окружения:
```
TELEGRAM_BOT_TOKEN=your_bot_token_here
OPENROUTER_API_KEY=your_openrouter_api_key_here
DATABASE_URL=postgresql://user:password@localhost:5432/wordmeaning
```

4. Запустите бота:
```bash
python3 main.py
```

## Деплой на Railway

### Шаг 1: Создайте PostgreSQL сервис

1. Зайдите в проект на [Railway.app](https://railway.app)
2. Нажмите **+ New** → **Database** → **PostgreSQL**
3. Railway автоматически создаст переменную `DATABASE_URL`

### Шаг 2: Настройте бот-сервис

1. Подключите GitHub репозиторий (если еще не подключен)
2. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN` - токен от @BotFather
   - `OPENROUTER_API_KEY` - API ключ от OpenRouter
   - `DATABASE_URL` - **автоматически добавится** после подключения к PostgreSQL

### Шаг 3: Подключите базу данных к боту

1. В настройках бот-сервиса → **Variables**
2. Нажмите **+ Reference** → выберите PostgreSQL сервис
3. Выберите переменную `DATABASE_URL`
4. Сохраните и подождите автоматического редеплоя

✅ **Готово!** Теперь данные сохраняются в отдельной базе и не удаляются при деплоях.

## Деплой на Render

1. Создайте аккаунт на [Render.com](https://render.com)
2. Создайте новый Web Service из GitHub репозитория
3. Добавьте переменные окружения
4. Деплой произойдет автоматически

## Деплой через Docker

### Локальный запуск с Docker Compose

Docker Compose автоматически запустит PostgreSQL и бота вместе:

1. Убедитесь, что у вас установлен Docker и Docker Compose
2. Создайте файл `.env` с переменными окружения:
```env
TELEGRAM_BOT_TOKEN=your_bot_token
OPENROUTER_API_KEY=your_openrouter_key
```

3. Запустите все сервисы:
```bash
docker-compose up -d
```

4. Просмотр логов:
```bash
docker-compose logs -f bot
```

5. Остановка всех сервисов:
```bash
docker-compose down
```

6. Удаление данных (⚠️ удалит все слова):
```bash
docker-compose down -v
```

### Сборка и запуск Docker образа вручную

Если у вас уже есть PostgreSQL сервер:

```bash
# Сборка образа
docker build -t wordmeaning-bot .

# Запуск контейнера
docker run -d \
  --name wordmeaning-bot \
  --restart unless-stopped \
  -e TELEGRAM_BOT_TOKEN="your_token" \
  -e OPENROUTER_API_KEY="your_key" \
  -e DATABASE_URL="postgresql://user:password@host:5432/dbname" \
  wordmeaning-bot
```

### Деплой на VPS с Docker

1. Склонируйте репозиторий на сервер:
```bash
git clone https://github.com/nevatas/wordmeaning.git
cd wordmeaning
```

2. Создайте `.env` файл с вашими ключами

3. Запустите через docker-compose:
```bash
docker-compose up -d
```

4. Настройте автоматическое обновление при пуше (опционально):
```bash
# Установите webhook или используйте GitHub Actions
```
